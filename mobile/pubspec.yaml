name: garagem_mobile
description: Aplicativo mobile da comunidade de projetos automotivos.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.11.0
  flutter_secure_storage: ^10.3.1
  crop_your_image: ^2.0.0
  image_picker: ^1.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
